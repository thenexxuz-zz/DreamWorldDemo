# DreamWorldDemo

## Laravel
#### Setup 
```
cd code-laravel
cp .env.example .env
composer install
yarn
yarn dev
touch database/database.sqlite
chmod 777 database/database.sqlite
chmod 777 database
chmod -R 777 storage
php artisan migrate
php artisan serve --port=9000
```

## Symfony
#### Setup
```
Not completed
```